// Posisi awal burung di sumbu X
const double birdStartX = 100;

// Posisi awal burung di sumbu Y
const double birdStartY = 100;

// Lebar burung
const double birdWidth = 60;

// Tinggi burung
const double birdHeight = 40;

// Gaya gravitasi untuk menarik burung ke bawah
const double gravity = 800;

// Kekuatan lompatan burung (negatif untuk melawan gravitasi)
const double jumpStrength = -300;

// Ketinggian tanah dalam game
const double groundHeight = 200;

// Kecepatan scroll tanah (pergerakan tanah)
const double groundScrollingSpeed = 100;

// Interval waktu antara pipa-pipa yang muncul
const double pipeInterval = 2;

// Jarak antara dua pipa secara vertikal (ruang untuk burung melewati)
const double pipeGap = 200;

// Ketinggian minimal pipa
const double minPipeHeight = 50;

// Lebar pipa
const double pipeWidth = 60;
